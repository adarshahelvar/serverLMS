This is a .env file Dummy copy:

PORT = 8000

ORIGIN = ['http://localhost:3000'] or Deploymeny URL

NODE_ENV = development or production


DB_URI = MongoDB URL

# in CLOUD_NAME, CLOUD_API_KEY, CLOUD_SECRET_API_KEY: the values of cloudinary is stored to store the image in cloudinary platform
CLOUD_NAME = Cloudinary name

CLOUD_API_KEY = Cloudinary api key

CLOUD_SECRET_API_KEY = Cloudinary secret key

REDIS_URL = Redis URL and For url after redis add one more s like (rediss://URL)

